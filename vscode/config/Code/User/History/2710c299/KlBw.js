async function router(req, res) {
    const url = new URL(req.url);

    switch(req.method) {
        case 'GET': {
            switch(req.path) {
                case 'contagem-pessoas'
            }
        }
    }
}

module.exports = router;